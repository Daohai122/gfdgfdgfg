(function () {
  "use strict";
  angular
    .module("MyApp")
    .controller("Report.Search", function (
      $rootScope,
      $stateParams,
      $state,
      $scope,
      $compile,
      ApiService,
      OptionService
    ) {

    });

})();
